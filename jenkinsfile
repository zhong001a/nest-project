pipeline {
    agent {
        docker { image 'nest-project-items'}
    }

    stages {
        stage('Build') {
            steps {
                sh 'docker-compose build'
            }
        }
        stage('Test') {
            steps {
                sh 'docker-compose up'
            }
        }

    }


}